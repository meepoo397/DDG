# guidances

```
.
├── random_ddg.py           # 整合所有使用else class作為Guided Score的Sampler Class(ex.RNDDG,FNDDG,CNDDG)
├── time_offset_guidance.py # TOG
├── cfg_adaptive_v*.py # 各種迭代中的 adaptive scheduler v13對應我們專題的版本
├── ej_ddg.py               # EJDDG
├── map_ddg_x0pred.py       # 使用x0 prediction based的MAP
├── map_ddg.py              # 未使用x0 prediction based的MAP
└── no_guidance.py          # Pure EDM2
````
